export interface UserT {
    id: number
    username: string
    isVerified: boolean
}

export interface AccountT {
    uid: number,
    login: string
    registeredAt: string
    displayName: string
    firstName: string
    secondName: string
}
